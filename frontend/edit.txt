"I need to implement user-specific workout splits in my GymTracker website. Currently, when users create workout splits, they are shared across all accounts instead of being private to each user.

Current Problem:

User A creates a workout split and logs out

User B creates a new account and logs in

User B can see User A's workout splits on both /splits and /your-workout-splits pages

All splits are showing as "YOUR CREATION" regardless of who created them

Required Solution:

Make workout splits completely user-specific and private

Each user should only see their own created splits

Use localStorage with user identification to store splits per user

Ensure splits are isolated between different user accounts

When a user logs out and a new user logs in, the new user should see zero splits initially

Technical Requirements:

Modify the splits storage system to include user identification

Update both /splits and /your-workout-splits pages to filter by current user

Implement proper user session management for splits

Use localStorage structure like: userSplits_${userId} or similar

Ensure complete data isolation between users

Expected Behavior:

User A creates splits → only User A sees them

User A logs out, User B logs in → User B sees empty splits list

User B creates splits → only User B sees their splits

No cross-contamination of workout splits between users

Please implement this user-specific workout splits system with proper authentication and data isolation."


So implement this has i mentioned so make it user specific and private and make sure that it is isolated between different user accounts and when a user logs out and a new user logs in the new user should see zero splits initially and also update both /splits and /your-workout-splits pages to filter by current user and implement proper user session management for splits and use localStorage structure like userSplits_${userId} or similar and ensure complete data isolation between users and expected behavior user A creates splits only user A sees them user A logs out user B logs in user B sees empty splits list user B creates splits only user B sees their splits no cross-contamination of workout splits between users please implement this user-specific workout splits system with proper authentication and data isolation